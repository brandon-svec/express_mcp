/**
 * Copy this file to config.js (gitignored) before running live Gemini tests.
 *
 *   cp tests/live/config.example.js tests/live/config.js
 *
 * Then set apiKey. Run only via: npm run test:live
 */

export const liveGeminiConfig = {
  apiKey: '',
  model: 'gemini-2.5-flash',
};
